extends layout

block content
  h1= title

  ul
    each producttype in producttype_list
      li
        a(href=producttype.url) #{producttype.name}
    else
      li There are no product types in this inventory.